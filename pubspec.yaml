name: clean_arch_tdd_ex
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Service locator
  get_it: ^2.0.1
  # Bloc for state management
  flutter_bloc: ^7.1.0
  # Value equality
  equatable: ^2.0.3
  # Functional programming thingies
  dartz: ^0.10.0
  # Remote API
  connectivity: ^3.0.6
  http: ^0.13.3
  # Local cache
  shared_preferences: ^2.0.7
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.15

flutter:
  uses-material-design: true